---
title: Endpoint and Trunk Monitoring
description: Review the telephony-side state signals that can affect RocketAiFlow workflows.
---

# Endpoint and Trunk Monitoring

Workflow monitoring is incomplete if it ignores the telephony path. Endpoint and trunk state can
change before the workflow logic itself shows a problem.

RocketAiFlow uses an Asterisk-based telephony layer and is built for real SIP and PBX
environments. That makes endpoint and trunk monitoring a practical requirement, not just a nice
to-have dashboard area.

## What to watch

Useful monitoring areas include:

- endpoint state
- endpoint state timeline
- trunk readiness
- connection availability changes
- sudden failure or congestion patterns
- campaign behavior changes that line up with telephony events

## Why this matters

If answered calls drop or failed calls spike, the root cause may sit in the telephony layer rather
than in prompts, routing logic, or campaign configuration.

Teams should be able to ask:

- Did the endpoint or trunk state change at the same time the issue started?
- Was the telephony path ready when the campaign attempted to dial?
- Did congestion or failure events increase during a specific time window?

## A useful monitoring pattern

1. Watch endpoint state changes over time.
2. Compare those changes with campaign activity and call outcomes.
3. Confirm whether the issue is local to one endpoint, one trunk, or the wider calling path.
4. Escalate based on the layer that actually failed.

<ProductScreenshot
  src="/screenshots/docs/telephony-dashboard-overview.png"
  alt="Grafana dashboard showing endpoints by state, endpoint state timeline, and the endpoints table."
  caption="This telephony monitoring view shows the endpoint state summary, the state timeline, and the detailed endpoint list in one place."
/>

<div className="docs-panel-grid docs-panel-grid-two">
  <ProductScreenshot
    src="/screenshots/docs/telephony-endpoints-by-state.png"
    alt="Grafana panel showing endpoints by state."
    caption="Endpoints by state gives operators a fast count of unavailable, idle, and in-use telephony resources."
  />
  <ProductScreenshot
    src="/screenshots/docs/telephony-endpoint-state-timeline.png"
    alt="Grafana panel showing the endpoint state timeline."
    caption="The state timeline helps teams correlate endpoint readiness changes with campaign or call outcome shifts."
  />
</div>

## Implementation note

If the deployment includes Prometheus-based metrics collection, endpoint and trunk state can be
tracked through that stack and visualized in Grafana-style dashboards. The actual series and panel
design should reflect the real telephony implementation rather than a generic template.

## Related pages

<CardGroup cols={2}>
  <Card title="Monitoring and Visibility" href="/observe/monitoring-and-visibility">
    Return to the broader operational monitoring model.
  </Card>
  <Card title="Telephony Architecture" href="/deploy/telephony-architecture">
    Review how trunk-based connectivity and SIP / PBX fit shape telephony monitoring.
  </Card>
  <Card title="Logs and Troubleshooting" href="/observe/logs-drilldown">
    Use the troubleshooting flow when telephony state affects live performance.
  </Card>
</CardGroup>
